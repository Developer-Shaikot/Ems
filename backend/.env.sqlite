# Environment
NODE_ENV=development

# Database - SQLite (No PostgreSQL installation required)
DATABASE_URL="file:./dev.db"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=1h

# Server
PORT=3001
FRONTEND_URL=http://localhost:3000
